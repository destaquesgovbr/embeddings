name: Deploy Embeddings DAGs to Composer

on:
  push:
    branches: [main]
    paths:
      - 'dags/**'
      - 'src/embeddings_client/**'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: destaquesgovbr/reusable-workflows/.github/workflows/composer-deploy-dags.yml@v1
    with:
      dags_local_path: dags
      dags_bucket_subdir: embeddings
      plugins_local_path: src/embeddings_client
      check_imports: true
      rsync_exclude: 'requirements\.txt$'
